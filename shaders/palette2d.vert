#version 430
// Vertex stage for the 2D palette pass. Matches raylib's attribute and
// uniform names so the pass can ride raylib's batch (BeginShaderMode).
in vec3 vertexPosition;
in vec2 vertexTexCoord;
in vec4 vertexColor;
uniform mat4 mvp;
out vec2 fragTexCoord;
out vec4 fragColor;
void main() {
    fragTexCoord = vertexTexCoord;
    fragColor = vertexColor;
    gl_Position = mvp * vec4(vertexPosition, 1.0);
}
